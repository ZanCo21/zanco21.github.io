/** @type {import('next').NextConfig} */
const nextConfig = {
 // Enable static HTML export
 output: {
  // Set exportTrailingSlash to true if you want trailing slashes in exported paths.
  exportTrailingSlash: true,
},
}

module.exports = nextConfig